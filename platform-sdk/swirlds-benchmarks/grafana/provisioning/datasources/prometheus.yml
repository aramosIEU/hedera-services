apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://node_0:9999
    jsonData:
      timeInterval: "5s"  # Optional: Adjust the time interval as needed
